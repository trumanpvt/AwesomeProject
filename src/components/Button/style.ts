import {StyleSheet} from 'react-native';

export default (colors: any) =>
  StyleSheet.create({
    container: {
      flex: 1,
      paddingTop: 20,
    },
    text: {
      fontSize: 20,
      textAlign: 'center',
    },
  });
